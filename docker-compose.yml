version: '3.8'

services:
  reelforge:
    build: .
    container_name: reelforge_ai
    restart: always
    ports:
      - "8000:8000"
    volumes:
      - ./storage:/app/storage
      - ./.env:/app/.env
    environment:
      - PYTHONUNBUFFERED=1
